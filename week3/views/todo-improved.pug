doctype html
html(lang="ko")
    head
        title Math
        link(rel="stylesheet", href="/style-improved.css")
        if error
            script.
                alert(#{'\'' + error + '\''});
        if result
            script.
                alert("합 연산 결과: #{result}");
        
    body
        main.container
            header
                h1
                    span.icon ✓
                    |  M A T H

            section.add-section
                form(action="/add-task", method="POST")
                    input(type="text", name="numA", placeholder="A", required=true, autocomplete="off")
                    input(type="text", name="numB", placeholder="B", required=true, autocomplete="off")
                    button(type="submit") 추가

            section.task-section
                if (!tasks || !tasks.length)
                    div.empty-state 
                        p.empty-icon ☁️
                        p.empty-message 할 일이 없습니다
                else
                    ul#task-list
                        each task, index in tasks
                            li(class=task.completed ? 'task-item completed' : 'task-item')
                                span.task-name #{task.name}
                                if !task.completed
                                    form(action="/complete-task", method="POST")
                                        input(type="hidden", name="taskIndex", value=index)
                                        button.complete-btn(type="submit") 완료
                                else
                                    span.done-badge 완료됨
